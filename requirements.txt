fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0
python-dotenv>=1.0.0
python-docx>=1.1.2
# fastapi.testclient requires httpx; <0.28 keeps the app= shortcut this FastAPI needs
httpx>=0.27,<0.28
# SSH public key parsing and signature verification (app/services/ssh_keys.py).
# Hand-rolling RSA padding checks is the classic way to build an auth bypass.
cryptography>=42.0.0
